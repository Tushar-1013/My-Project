<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Admin Dashboard</title>
    <link rel="stylesheet" href="stylesheets/styles.css" />
    <link rel="stylesheet" href="stylesheets/demo.css" />
  </head>
  <body>
    <div class="dashboard">
      <aside class="sidebar">
        <h2>Admin</h2>
        <nav>
          <ul>
            <li onclick="renderView('addProduct')">Add Product</li>
            <li onclick="renderView('editProduct')">Edit Product</li>
            <li onclick="renderView('deleteProduct')">Delete/Update</li>
            <li onclick="renderView('addBanner')">Add Banner</li>
          </ul>
        </nav>
      </aside>
      <main class="main-view">
        <div id="content">
          <h2>Welcome, Admin</h2>
          <p>Select an option from the left to get started.</p>
        </div>
      </main>
    </div>
    <script src="/scripts/script.js"></script>
  </body>
</html>
